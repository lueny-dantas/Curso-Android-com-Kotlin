<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="paixao.lueny.curso_android_kotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cursoandroidkotlin"
        tools:targetApi="31">
        <activity
            android:name=".activity.ActivityUserRegistration"
            android:label="Cadastrar usúario" />

        <activity
            android:name=".activity.ActivityLogin"
            android:exported="true"
            android:theme="@style/Theme.Cursoandroidkotlin.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ActivityProductsList" />
        <activity android:name=".activity.ActivityProductDetails" />
        <activity android:name=".activity.ActivityProductForm" />


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>